public class LinkedList {

    private class Node {
        private int data;
        Node nexNode;

        public Node(int data) {
            this.data = data;
            this.nexNode = null;
        }
    }
}